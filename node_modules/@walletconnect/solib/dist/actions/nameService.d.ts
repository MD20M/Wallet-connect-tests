import type { PublicKey } from '@solana/web3.js';
export interface FetchNameArgs {
    address: string;
}
export declare type FetchNameResult = {
    domain: PublicKey;
    reverse: string;
} | null;
export declare function getSolDomainsFromPublicKey(address: string): Promise<string[] | null>;
/**
 * Retrieves a domain name to display for a user if any
 * First it attempts to get the favorite domain.
 * This feature doesn't appear to commonly be used hence
 * if none is set we try to capture other domains associated to the
 * account and use the first one that pops up.
 * @param address Base 58 encoded address
 * @returns
 */
export declare function fetchName(address: string): Promise<FetchNameResult>;
export declare function fetchAddressFromDomain(domain: string): Promise<string | null>;
